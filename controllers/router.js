var index = require('./index');
var auth = require('./auth');
var users = require('./users');
var hikes = require('./hikes');

module.exports = {
	index : index,
	auth: auth,
	users: users,
	hikes: hikes
}